<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/statusView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Starting…"
                android:textSize="13sp"
                android:textStyle="italic"
                android:textColor="@color/status_text"
                android:maxLines="2"
                android:ellipsize="end" />

            <Button
                android:id="@+id/btnNew"
                style="?android:attr/borderlessButtonStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:textSize="18sp"
                android:contentDescription="New chat"
                android:text="＋" />

            <Button
                android:id="@+id/btnChats"
                style="?android:attr/borderlessButtonStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:textSize="18sp"
                android:contentDescription="Chat history"
                android:text="☰" />

            <Button
                android:id="@+id/btnSettings"
                style="?android:attr/borderlessButtonStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:textSize="18sp"
                android:contentDescription="Settings"
                android:text="⚙" />
        </LinearLayout>

        <Button
            android:id="@+id/btnImport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:visibility="gone"
            android:background="@drawable/bg_banner"
            android:textColor="@color/banner_text"
            android:paddingTop="14dp"
            android:paddingBottom="14dp"
            android:text="📦 Import model pack" />

        <ScrollView
            android:id="@+id/chatScroll"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="8dp"
            android:clipToPadding="false"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/messagesView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingBottom="88dp" />
        </ScrollView>
    </LinearLayout>

    <!-- Floating action cluster: file-import (small) + record (primary), bottom-right. -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/btnFile"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginEnd="14dp"
            android:enabled="false"
            android:elevation="4dp"
            android:background="@drawable/bg_fab_secondary"
            android:textColor="@color/fab_secondary_fg"
            android:textSize="20sp"
            android:contentDescription="Answer from an audio file"
            android:text="📁" />

        <FrameLayout
            android:layout_width="64dp"
            android:layout_height="64dp">

            <Button
                android:id="@+id/btnRecord"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:enabled="false"
                android:elevation="6dp"
                android:background="@drawable/bg_fab_primary"
                android:textColor="@color/brand_on_primary"
                android:textSize="24sp"
                android:contentDescription="Record"
                android:text="🎙" />

            <ProgressBar
                android:id="@+id/recordProgress"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:visibility="gone"
                android:indeterminateTint="@color/brand_on_primary"
                android:elevation="7dp" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
